extends ./layout.pug

block content
  h1#title Starfire
  .menu#profile
    h3.formHeader Pilot profile: #{callsign}
    .profile
      h3 Wins: 
      h3 #{wins}
    .profile
      h3 Losses: 
      h3 #{losses}
    .profile
      h3 Rank: 
      h3 #{rank}
    .link
      a(href='/menu')
        button REPORT FOR DUTY
    .link
      p 
        a(href='/logout') Log out
        
block js
  include ./partials/_auth.pug
